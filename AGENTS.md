# Core Architectural Conventions & Modularity Rules

## 1. Strict One-System-Per-File Modularity (กฎการแยกระบบเป็นไฟล์เดี่ยว)
- **แยกไฟล์ทุกระบบทันที (Modularization)**: เมื่อมีการสร้างหรือเพิ่มระบบใหม่ (Module/Feature/Tool/Editor) ไม่ว่าจะเล็กหรือใหญ่ **ห้าม**เขียนโค้ดรวมไว้ในไฟล์หลักเช่น `App.tsx` หรือไฟล์กลางเด็ดขาด ให้สร้างไฟล์ `.tsx` แยกออกมาใน `src/components/` ทันทีตามชื่อระบบนั้นๆ (เช่น ระบบ Map Edit ให้สร้าง `MapEdit.tsx`, ระบบ Sound/Audio ให้สร้าง `AudioEditor.tsx` หรือ `VoiceMusicStudio.tsx`)
- **App.tsx มีหน้าที่เพียง Router & Navigation**: ไฟล์ `App.tsx` ควรทำหน้าที่แค่ import ไฟล์ระบบต่างๆ เข้ามา และเรนเดอร์ผ่านหน้าจอเมนูหลักหรือ Command Palette เท่านั้น ห้ามใส่ตรรกะซับซ้อนของระบบย่อยลงใน `App.tsx`
- **การเพิ่มระบบใหม่ในอนาคต**: ทุกครั้งที่ผู้ใช้สั่งเพิ่มระบบ/ฟีเจอร์ใหม่ ให้สร้างไฟล์ใหม่แยกออกมาทันที แล้วค่อยดึงเข้ามาเสียบในรายการเครื่องมือ (`tools` array) ใน `App.tsx`

## 2. Strict 1 Node / 1 Module = 1 Dedicated File Rule (กฎ 1 โหนด / 1 โมดูล = 1 ไฟล์เดี่ยวแยกกัน)
- **การเขียนหรือพัฒนาเกม/โปรแกรม (Game & Software Architecture)**: เมื่อ AI หรือระบบทำการเขียนและพัฒนาเกมหรือระบบย่อยใดๆ **ต้องแยกทุกระบบออกเป็นไฟล์ย่อยๆ เสมอ** โดยยึดหลัก **1 โหนด หรือ 1 โมดูล = 1 ไฟล์เดี่ยว**
  - ตัวอย่าง: หากทำระบบ Combat จะต้องไม่รวมโค้ดในไฟล์เดียว แต่ต้องแยกเป็น `DamageCalculationModule.ts`, `StaminaDrainNode.ts`, `ComboHitCounterNode.ts`, `CombatFeedbackVFXNode.ts`, `CombatStateRepository.ts`
  - แม้ว่าจำนวนไฟล์ในโปรเจกต์จะเยอะขึ้น แต่จะทำให้โครงสร้างโค้ดสะอาด เป็นระเบียบ สามารถหาจุดผิดพลาด และเข้ามาแก้ไขทีละไฟล์ได้ง่ายมาก โดยไม่กระทบส่วนอื่น
- **การตั้งชื่อไฟล์อย่างตรงไปตรงมาและเกี่ยวข้อง (Descriptive System-Specific Naming)**:
  - ชื่อไฟล์ต้องสะท้อนถึงระบบหลัก (System) และหน้าที่ของโมดูลนั้น (Module Function) อย่างชัดเจนเสมอ เช่น `InventorySlotController.ts`, `QuestDialogueNodeEvaluator.ts`, `SpatialOctreeNode.ts`, `WeatherParticleEmitterModule.ts`
  - ห้ามตั้งชื่อไฟล์แบบกำกวมหรือไม่สื่อความหมาย เช่น `helper.ts`, `misc.ts`, `temp.ts`, `node1.ts`
- **การเขียนคำอธิบายอย่างละเอียดในทุกไฟล์ (Exhaustive In-File Documentation & Comments)**:
  - ทุกๆ ไฟล์ที่สร้างขึ้น **ต้องมีบล็อก Header Documentation อธิบายอย่างละเอียดลึกซึ้ง** (ทั้งภาษาไทยและอังกฤษ) ครอบคลุม:
    1. **วัตถุประสงค์และหน้าที่ของไฟล์/โมดูลนี้ (Module Purpose & Responsibility)**
    2. **สถาปัตยกรรมและการเชื่อมโยงกับระบบอื่น (Architecture & System Integration)**
    3. **พารามิเตอร์ Input / Output ที่รับส่ง (Inputs, Outputs & Data Contracts)**
    4. **การจัดการข้อผิดพลาดและ Edge Cases (Error Handling & Fallbacks)**
    5. **ตัวอย่างการเรียกใช้งาน (Usage Example)**
  - ภายในฟังก์ชัน, เมธอด, และตัวแปรสำคัญ ต้องมี JSDoc / Docstring และคอมเมนต์อธิบายตรรกะทีละสเต็ปอย่างละเอียด เพื่อให้เข้าใจง่ายและปรับแต่งได้ทันที

## 3. Token Limit & Error Prevention Strategy
- **แก้ปัญหา Token ล้น/โค้ดขาดตอน**: เนื่องจากแอปพลิเคชันมีขนาดใหญ่และมีเครื่องมือกว่า 100 ระบบ การเขียนไฟล์เดียวขนาดใหญ่จะทำให้ระบบตัดจบกลางทาง (Token Limit Exceeded) ได้ง่าย ดังนั้นต้องกระจายโค้ดออกเป็นไฟล์ย่อยๆ เสมอ
- **ตรวจสอบความถูกต้อง (Lint & Build)**: หลังการแก้ไขโค้ดแต่ละชุด ให้รัน `lint_applet` และ `compile_applet` เพื่อเช็คข้อผิดพลาดทีละไฟล์ ป้องกันระบบพังกระทบกันเป็นวงกว้าง

## 4. High-Fidelity & Professional Execution Rule (กฎความละเอียดระดับมืออาชีพ)
- **ความละเอียดมหาศาล (Massive Detail-Oriented)**: ทุกครั้งที่ทำงานสร้างหรือปรับแก้ระบบ ต้องคิดแบบนักพัฒนามืออาชีพ (AAA Game Developer) ใส่ใจทุกรายละเอียดของ UI, UX, สถาปัตยกรรมโค้ด, และการจัดการ Error อย่างถึงที่สุด
- **การเพิ่มเครื่องมือที่เกี่ยวข้อง (Holistic Tool Integration)**: ทุกครั้งที่มีการขอให้เพิ่ม "ระบบใหม่" ให้วิเคราะห์และเพิ่ม "เครื่องมือย่อยที่เกี่ยวข้องและจำเป็น" เข้าไปด้วยเสมอ (เช่น ขอระบบเซฟเกม ต้องแถมระบบตั้งค่าบีบอัดและเข้ารหัส หรือขอระบบ AI ต้องมี Blackboard และ Perception)
- **การเข้ากันได้ (Backward Compatibility)**: ระบบใหม่ที่เพิ่มเข้าไป **ต้องไม่ขัดแย้ง** หรือทำให้ระบบเก่าที่ทำงานอยู่แล้วพัง (Break) ต้องเชื่อมต่อโครงสร้างเดิมอย่างสมบูรณ์แบบ

## 5. Autonomous Debugging & Deep Testing (การทดสอบและแก้ไขข้อผิดพลาดขั้นสูง)
- **การแก้ไขทันที (Immediate Error Resolution)**: หากพบ Error หรือบัคในระบบระหว่างทำงาน ให้ดำเนินการแก้ไขทันทีโดยไม่ต้องรอคำสั่งเพิ่มเติม
- **การวิเคราะห์เชิงลึก (Deep Analytical Thinking)**: ต้องคิดวิเคราะห์อย่างละเอียดถี่ถ้วนในทุกขั้นตอนการทำงาน และใส่ใจในทุกกระบวนการอย่างสูงสุด
- **การทดสอบแบบครบวงจร (Exhaustive Testing)**: เมื่อได้รับคำสั่งให้ "Test" (ทดสอบ) ระบบ ให้ทำการทดสอบอย่างละเอียดถี่ถ้วนที่สุด ทั้งในระดับโครงสร้างโค้ด (Code-level) และระดับการใช้งานจริง (UX/Functional-level) และต้องทำการค้นหาพร้อมแก้ไขจุดบัคหรือ Error ที่พบทั้งหมดให้เสร็จสมบูรณ์
